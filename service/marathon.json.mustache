{
  "id": "{{service.name}}",
  "cpus": {{service.cpus}},
  "mem": {{service.mem}},

  "env": {
    "DCOS_SERVICE_NAME": "{{service.name}}"
  },

  "cmd": "cd \"$MESOS_SANDBOX\" && \"$FLINK_HOME/bin/mesos-appmaster.sh\" -Dmesos.initial-tasks={{service.taskmanagers}} -Dmesos.resourcemanager.tasks.mem={{service.taskmanager_memory}} -Dtaskmanager.numberOfTaskSlots={{service.slots}}",

  "container": {
    "type": "DOCKER",
    "docker": {
      "image": "{{resource.assets.container.docker.flink}}",
      "network": "HOST"
    }
  },

  "healthChecks": [
    {
      "protocol": "HTTP",
      "path": "/",
      "port": 8081,
      "gracePeriodSeconds": 5,
      "intervalSeconds": 60,
      "timeoutSeconds": 10,
      "maxConsecutiveFailures": 3
    }
  ]
}
